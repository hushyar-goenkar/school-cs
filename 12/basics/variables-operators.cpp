// Converted from basics.pdf

#include <iostream>
using namespace std;

int main() {
  int c; // Only declared
  int a = 21, b = 10; // Initialized + Declared

  // Basic operators
  c = a + b;
  c = a - b;
  c = a * b;
  c = a / b;

  cout << "\n"; // << is the output operator and << is the input operator
  // This is one of the WORST possible syntax

  return 0;
}
